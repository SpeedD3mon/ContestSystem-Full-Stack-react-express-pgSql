# 🐳 Fullstack Contest System (Dockerized)

## Requirements
- Docker
- Docker Compose

## Run Project
just get docker and run this command 
will lunch the client(react) for u on local 3000 port and server 5000 port  and db 5432 
db has init and  a seed sql with basic data  
```bash
docker compose up --build

added api yaml file has all the apis mention in it 